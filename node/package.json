{
  "name": "agent-forge",
  "version": "1.0.0",
  "description": "AgentForge — Incident Response System (Node.js Track)",
  "type": "module",
  "scripts": {
    "start": "node agent_forge.js",
    "start:cached": "node agent_forge.js --cached",
    "test": "vitest run",
    "verify": "node verify_setup.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "dotenv": "^16.4.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
